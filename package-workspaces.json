{
  "name": "fixmath-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@fixmath/core",
    "build": "npm run build --workspace=@fixmath/core"
  }
}
